<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end mb-5">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block">Logout</a>
        </div>
        <div class="w-full flex flex-col items-center">
           
            <h5 class="mb-5 text-center">Edit Your post</h5>
            <form action="/update/<%= post._id %>" method="post" class="w-full max-w-md flex flex-col items-center">
                <textarea placeholder="What's on your mind?" class="w-full resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none" name="content" id=""><%= post.content %></textarea>
                <input class="px-5 py-2 bg-yellow-500 text-black text-sm rounded-md mt-3 w-full max-w-xs cursor-pointer" type="submit" value="Update Post">
            </form>
        </div>
        
    </div>
    
</body>
</html>